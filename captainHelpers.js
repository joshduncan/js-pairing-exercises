export const firstNames = data => data;

export const firstNamesSorted = data => data;

export const firstNamesSortedOnlyJs = data => data;

export const totalAge = data => data;

export const fullNamesByAge = data => data;
